type Brand {
  id: ID!
  name: String!
  slug: String!
  description: String
  # createdAt: Date
}

extend type Query {
  brands: [Brand]!
  brand(id: String!): Brand
}

extend type Mutation {
  addBrand(name: String!, description: String): Brand!
  deleteBrand(id: ID): Boolean!
}
